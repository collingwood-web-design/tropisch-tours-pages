Logo files

Primary asset: tropisch tours and travel logo-white.svg
Used as a CSS mask in the header — colour is controlled via --logo-color in CSS.

Examples:
  Header over hero:  --logo-color: #ffffff;
  Solid header:      --logo-color: var(--color-primary);
  Accent variant:    --logo-color: #e67520;

The .ai and .png exports are kept for reference or print use.
